<?xml version="1.0" encoding="utf-8"?>

<ContentView
    x:Class="MuscleCuties.App.Controls.Cycle.CycleDatePhaseModal"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:cycleModels="clr-namespace:MuscleCuties.Core.Models.UI.Cycle;assembly=MuscleCuties.Core"
    xmlns:sharedControls="clr-namespace:MuscleCuties.App.Controls.Shared"
    xmlns:vm="clr-namespace:MuscleCuties.Core.ViewModels.Cycle;assembly=MuscleCuties.Core"
    x:DataType="vm:CycleViewModel"
    IsVisible="{Binding IsDatePhaseModalVisible}">

    <Grid
        BackgroundColor="{StaticResource ModalScrim}"
        Padding="20,56,20,36">
        <Grid.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding CloseDatePhaseModalCommand}" />
        </Grid.GestureRecognizers>
        <Border
            Style="{StaticResource CardPrimary}"
            Padding="0"
            Margin="0"
            MaximumHeightRequest="720"
            VerticalOptions="Center">

            <Grid RowDefinitions="Auto, *, Auto">
                <Grid
                    Grid.Row="0"
                    RowDefinitions="Auto, Auto"
                    RowSpacing="12"
                    Padding="20,18,20,12">

                    <Grid ColumnDefinitions="*, Auto" ColumnSpacing="12">
                        <Grid RowDefinitions="Auto, Auto" RowSpacing="6">
                            <Label
                                Text="{Binding SelectedCalendarDateText}"
                                Style="{StaticResource SubHeadline}"
                                FontSize="24" />

                            <Label
                                Grid.Row="1"
                                Text="{Binding SelectedCalendarPhaseText}"
                                Style="{StaticResource MutedLabel}" />
                        </Grid>

                        <Border
                            Grid.Column="1"
                            WidthRequest="44"
                            HeightRequest="44"
                            StrokeShape="RoundRectangle 22"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray900}}"
                            StrokeThickness="0"
                            VerticalOptions="Start"
                            SemanticProperties.Description="Close cycle date editor">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding CloseDatePhaseModalCommand}" />
                            </Border.GestureRecognizers>
                            <Path
                                Data="M6 6L18 18M18 6L6 18"
                                Stroke="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}"
                                StrokeThickness="2.2"
                                StrokeLineCap="Round"
                                Aspect="Uniform"
                                WidthRequest="18"
                                HeightRequest="18"
                                HorizontalOptions="Center"
                                VerticalOptions="Center" />
                        </Border>
                    </Grid>

                    <BoxView
                        Grid.Row="1"
                        HeightRequest="1"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}"
                        Opacity="0.7" />
                </Grid>

                <ScrollView Grid.Row="1">
                    <VerticalStackLayout
                        Padding="20,0,20,16"
                        Spacing="12"
                        BindableLayout.ItemsSource="{Binding PhaseEditOptions}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="cycleModels:CyclePhaseOptionItem">
                                <Border
                                    BackgroundColor="{Binding BackgroundColor}"
                                    StrokeShape="RoundRectangle 14"
                                    Stroke="{Binding TextColor}"
                                    StrokeThickness="0"
                                    Padding="16,14"
                                    MinimumHeightRequest="60"
                                    SemanticProperties.Description="{Binding Name, StringFormat='Select {0} phase'}">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding BindingContext.SelectPhaseOptionCommand, Source={x:Reference Root}}"
                                            CommandParameter="{Binding .}" />
                                    </Border.GestureRecognizers>
                                    <Border.Triggers>
                                        <DataTrigger
                                            TargetType="Border"
                                            Binding="{Binding IsSelected}"
                                            Value="True">
                                            <Setter Property="StrokeThickness" Value="1.5" />
                                        </DataTrigger>
                                    </Border.Triggers>

                                    <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                        <sharedControls:PhaseVisualView
                                            Source="{Binding IconSource}"
                                            WidthRequest="34"
                                            HeightRequest="34"
                                            VerticalOptions="Center" />

                                        <Grid Grid.Column="1" RowDefinitions="Auto, Auto" RowSpacing="3">
                                            <Label
                                                Text="{Binding Name}"
                                                TextColor="{Binding TextColor}"
                                                FontSize="15"
                                                FontAttributes="Bold" />
                                            <Label
                                                Grid.Row="1"
                                                Text="{Binding Description}"
                                                TextColor="{Binding TextColor}"
                                                FontSize="12"
                                                Opacity="0.75" />
                                        </Grid>

                                        <Border
                                            Grid.Column="2"
                                            WidthRequest="26"
                                            HeightRequest="26"
                                            StrokeShape="RoundRectangle 13"
                                            Stroke="{Binding TextColor}"
                                            StrokeThickness="1.4"
                                            BackgroundColor="Transparent"
                                            VerticalOptions="Center">
                                            <Border.Triggers>
                                                <DataTrigger
                                                    TargetType="Border"
                                                    Binding="{Binding IsSelected}"
                                                    Value="True">
                                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                                </DataTrigger>
                                            </Border.Triggers>
                                            <Label
                                                Text="&#x2713;"
                                                TextColor="{StaticResource White}"
                                                FontSize="13"
                                                FontAttributes="Bold"
                                                HorizontalOptions="Center"
                                                VerticalOptions="Center"
                                                IsVisible="{Binding IsSelected}" />
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </ScrollView>

                <Grid
                    Grid.Row="2"
                    RowDefinitions="Auto, Auto, Auto"
                    RowSpacing="12"
                    Padding="20,0,20,20">
                    <Label
                        Text="{Binding PhaseEditStatusText}"
                        Style="{StaticResource ErrorLabel}"
                        IsVisible="{Binding HasPhaseEditStatus}" />

                    <Border
                        Grid.Row="1"
                        IsVisible="{Binding HasPhaseJumpWarning}"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource Gray900}}"
                        Stroke="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource SecondaryDarkText}}"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 18"
                        Padding="16,14">
                        <Grid RowDefinitions="Auto, Auto, Auto" RowSpacing="10">
                            <Label
                                Text="{Binding PhaseJumpWarningTitle}"
                                Style="{StaticResource CardTitle}" />
                            <Label
                                Grid.Row="1"
                                Text="{Binding PhaseJumpWarningText}"
                                Style="{StaticResource MutedLabel}" />
                            <Grid Grid.Row="2" ColumnDefinitions="*, *" ColumnSpacing="10">
                                <Button
                                    Text="Use next phase"
                                    Command="{Binding UseSuggestedPhaseCommand}"
                                    IsVisible="{Binding HasSuggestedPhase}"
                                    HeightRequest="48"
                                    FontSize="13" />
                                <Button
                                    Grid.Column="1"
                                    Text="Forgot to log shift"
                                    Command="{Binding ReviewEarlierPhaseRecordsCommand}"
                                    HeightRequest="48"
                                    FontSize="13"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}"
                                    TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                            </Grid>
                        </Grid>
                    </Border>

                    <Button
                        Grid.Row="2"
                        Text="Save phase"
                        Command="{Binding SaveDatePhaseCommand}"
                        HeightRequest="52"
                        IsVisible="{Binding HasPhaseJumpWarning, Converter={StaticResource InvertedBoolConverter}}" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</ContentView>
